steps:
- name: 'gcr.io/cloud-builders/gsutil'
  entrypoint: 'sh'
  args:
  - '-c'
  - |
    gsutil cp gs://binary_storage/sample_go_build .
    chmod 777 sample_go_build
    ls -l
    $BUILD_WORKSPACE_DIRECTORY/sample_go_build check product d1 d2

